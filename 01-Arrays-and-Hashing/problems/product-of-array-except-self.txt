Pattern: Arrays & Prefix Products (no division)
Problem: Product of Array Except Self
Explanation: Build prefix and suffix product arrays so each position can be computed in O(1) without using division, for overall O(n) time and O(1) extra space.
